import EmConstrucao from '@/components/EmConstrucao'

/**
 * Placeholder do bloco 4.5 (detalhe). Existe agora só pra que a linha
 * clicável da listagem não caia em 404.
 */
export default function PropostaDetalhePage() {
  return <EmConstrucao pagina="Proposta" />
}
